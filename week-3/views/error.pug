extends layout.pug

block content
    header
        h1= `${error.message}`
        h2= `Status: ${error.status}`
        pre= error.stack

    section#content
        p &#40;&acute;;&omega;;`&#41;ｳｩｩ